
//header file guard "to avoid re-include and redefinition of any variable //include once "
# ifndef STD_TYPES //"unique for each file so we make it with file name "
# define STD_TYPES
typedef unsigned char u8;
typedef unsigned short int u16;
typedef unsigned long int u32;
typedef unsigned long long int u64;

typedef signed char s8; //8 no of bits
typedef signed short int s16;
typedef signed long int s32;
typedef signed long long int s64;

typedef float f32;
typedef double f64;
typedef long double f128;
# endif
